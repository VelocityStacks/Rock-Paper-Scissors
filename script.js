// window.alert();
// window.confirm();
// window.prompt();

var wins = 0;

var function(){

}

var RPS =["Rock", "Paper", "Scissors"];
// computer input

var playerInput = window.prompt("Pick one of the three: Rock, Paper, or Scissors");
window.alert(playerInput);

if 


function RPS


function playGame (){
    // prompt for RPS
    // generate computer RPS
    // check who won
    // increase stats
    // display stats
    var playAgain = window.confirm("Play again?")
    if (playAgain) {
        playGame();
    }
};

playGame();